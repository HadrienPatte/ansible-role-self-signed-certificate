---
dependency:
  name: galaxy
driver:
  name: docker
lint:
  name: yamllint
  options:
    config-data:
      extends: default
      rules:
        line-length:
          level: warning
          max: 100
platforms:
  - name: debian
    image: debian
  - name: fedora
    image: fedora
provisioner:
  name: ansible
  lint:
    name: ansible-lint
scenario:
  name: default
verifier:
  name: testinfra
  lint:
    name: flake8
...
